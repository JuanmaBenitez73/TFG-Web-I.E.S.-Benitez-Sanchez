{% extends 'base.html.twig' %}

{% block title %}OrganizationChart{% endblock %}

{% block body %}
    <h1>OrganizationChart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organization_chart.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ organization_chart.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ organization_chart.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ organization_chart.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_organization_chart_index') }}">back to list</a>

    <a href="{{ path('app_organization_chart_edit', {'id': organization_chart.id}) }}">edit</a>

    {{ include('organization_chart/_delete_form.html.twig') }}
{% endblock %}
