<form method="post" action="{{ path('app_fp_delete', {'id': fp.id}) }}" id="delete-fp-form">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fp.id) }}">
	<button type="button" class="delete-button py-2 px-3 rounded text-white" data-bs-toggle="modal" data-bs-target="#confirmDeleteFPModal">
		<i class="fas fa-trash"></i>
		Delete
	</button>
</form>

<div class="modal fade" id="confirmDeleteFPModal" tabindex="-1" aria-labelledby="confirmDeleteFPModalLabel" aria-hidden="true">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="confirmDeleteFPModalLabel">Confirmar borrado</h5>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
			</div>
			<div class="modal-body">
				¿Seguro que quieres borrar esta noticia? Esta acción no se puede deshacer.
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
				<button type="button" class="btn btn-danger" id="confirm-delete-fp-button">Sí, borrar</button>
			</div>
		</div>
	</div>
</div>

 <script>
document.getElementById('confirm-delete-fp-button').addEventListener('click', function () {
    document.getElementById('delete-fp-form').submit();
});
</script>
